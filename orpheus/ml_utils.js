'use strict'
const conf = require('../config');

let mod = module.exports = {};

mod.parseTracks = function(tracksObject) {
  let parsed = [];
  for (let i = 0; i < tracksObject.length; i++) {
    
  }
};